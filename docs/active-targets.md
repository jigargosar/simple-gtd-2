- [x] remove archive (delete + confirm)
- [x] remove edit pencil (+ wider title click area)
- [x] remove section count

Animations:
- [ ] exit inherits entrance stagger delay — slide-out waits up to 360ms (section) / 240ms (task) before moving
- [ ] delete depends on animationend firing — fragile (hidden tab / interruption); decide: delete-on-click vs keep animation-gated
- [ ] checkbox check animates in (dot-pop) but not out — asymmetric
- [ ] task-out keyframe drives section exits via .anim-out — naming

Hover:
- [ ] section header row has no hover bg (task rows do)
- [ ] add-rows use focus-within bg-stone-100/40 vs task hover bg-stone-100/60 — trigger + opacity mismatch

Docs:
- [ ] delete stale archive docs: docs/reference/archive-visibility.md, docs/archive-views-mock.html

---
Later:
- [ ] dnd
